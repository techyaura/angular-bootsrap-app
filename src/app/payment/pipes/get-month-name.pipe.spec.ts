import { GetMonthNamePipe } from './get-month-name.pipe';

describe('GetMonthNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMonthNamePipe();
    expect(pipe).toBeTruthy();
  });
});
